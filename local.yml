---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install packages
    package:
      name: 
        - htop
        - tmux
        - vim-nox

  - name: copy walpaper file 
    copy: 
      src: files/walpaper.png
      dest: /usr/share/backgrounds/ansible-walpaper.png
      owner: root
      group: root
